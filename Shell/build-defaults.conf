# shellcheck shell=bash
#
# Generated from config/project.json; edit the canonical JSON source instead.
# Generated by tools/gen-project-config.mjs.
#
# 构建默认值统一配置 / Unified build defaults.
# 只允许纯 bash 变量赋值 / Plain Bash parameter defaults only.

: "${WRT_LAN_IP:=192.168.1.1}"
: "${WRT_ZONENAME:=Asia/Shanghai}"
: "${WRT_TIMEZONE:=CST-8}"
: "${WRT_THEME:=luci-theme-argon}"
: "${WRT_NTP_ID:=cn}"
: "${WRT_NTP_1:=ntp.aliyun.com}"
: "${WRT_NTP_2:=time1.cloud.tencent.com}"
: "${WRT_NTP_3:=cn.ntp.org.cn}"
: "${WRT_NTP_4:=cn.pool.ntp.org}"
: "${WRT_PACKAGE_MIRROR_ID:=source-default}"
: "${WRT_PASSWORD_MODE:=prompt}"
: "${WRT_DEFAULT_TAG:=anonymous}"
: "${WRT_COMPILE_JOBS:=auto}"
: "${WRT_DOWNLOAD_JOBS:=auto}"
: "${WRT_PUBLIC_ACTIVE_BUILDS:=3}"
