# 私有文档目录,不上传;docs/ 里的全部公开 / private docs stay local; everything under docs/ ships
docs-private/

# 本地工具与依赖 / local tooling & deps
node_modules/
.claude/
.vscode/

# Codex local conversation attachments; never commit / Codex 对话附件,不提交
/.codex-remote-attachments/
